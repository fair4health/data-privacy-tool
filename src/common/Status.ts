/**
 * App-wide status types
 */

enum Status {
  SUCCESS = 'success',
  WARNING = 'warning',
  ERROR = 'error',
  IN_PROGRESS = 'in-progress',
  PENDING = 'pending',
  LOADING = 'loading',
  DONE = 'done'
}

export default Status
